// import base from "../../../middlewares/common";
// import royalCourt, { getOneMember } from "../../../models/royalCourt";

// // async function handleDelete({ query: { id } }, res) {
// //   if (await uploadedDocument.deleteOneDocument(id)) res.status(204).send();
// //   else res.status(404).send("Document not found");
// // }

// async function handleGetOne({ query: { id } }, res) {
//   const member = await User.getOneMember(id);
//   if (member) res.status(200).send(member);
//   else res.status(404).send("Memner not found");
// }

// export default base().get(handleGetOne);
